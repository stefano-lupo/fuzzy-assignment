IF (clothes_type[silk] AND dirt_type[not_greasy]) AND dirtiness[small] THEN washing_time[very_short]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[silk] AND dirt_type[not_greasy]) AND dirtiness[medium] THEN washing_time[short]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[silk] AND dirt_type[not_greasy]) AND dirtiness[large] THEN washing_time[medium]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[silk] AND dirt_type[medium]) AND dirtiness[small] THEN washing_time[medium]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[silk] AND dirt_type[medium]) AND dirtiness[medium] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[silk] AND dirt_type[medium]) AND dirtiness[large] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[silk] AND dirt_type[greasy]) AND dirtiness[small] THEN washing_time[medium]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[silk] AND dirt_type[greasy]) AND dirtiness[medium] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[silk] AND dirt_type[greasy]) AND dirtiness[large] THEN washing_time[very_long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[woolen] AND dirt_type[not_greasy]) AND dirtiness[small] THEN washing_time[short]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[woolen] AND dirt_type[not_greasy]) AND dirtiness[medium] THEN washing_time[medium]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[woolen] AND dirt_type[not_greasy]) AND dirtiness[large] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[woolen] AND dirt_type[medium]) AND dirtiness[small] THEN washing_time[medium]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[woolen] AND dirt_type[medium]) AND dirtiness[medium] THEN washing_time[medium]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[woolen] AND dirt_type[medium]) AND dirtiness[large] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[woolen] AND dirt_type[greasy]) AND dirtiness[small] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[woolen] AND dirt_type[greasy]) AND dirtiness[medium] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[woolen] AND dirt_type[greasy]) AND dirtiness[large] THEN washing_time[very_long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[cotton] AND dirt_type[not_greasy]) AND dirtiness[small] THEN washing_time[short]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[cotton] AND dirt_type[not_greasy]) AND dirtiness[medium] THEN washing_time[medium]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[cotton] AND dirt_type[not_greasy]) AND dirtiness[large] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[cotton] AND dirt_type[medium]) AND dirtiness[small] THEN washing_time[medium]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[cotton] AND dirt_type[medium]) AND dirtiness[medium] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[cotton] AND dirt_type[medium]) AND dirtiness[large] THEN washing_time[very_long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[cotton] AND dirt_type[greasy]) AND dirtiness[small] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[cotton] AND dirt_type[greasy]) AND dirtiness[medium] THEN washing_time[long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

IF (clothes_type[cotton] AND dirt_type[greasy]) AND dirtiness[large] THEN washing_time[very_long]
	AND aggregation function : fmin
	OR aggregation function  : fmax

